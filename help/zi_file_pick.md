---
category: Picker
description: fd/find + fzf file picker
---
# zi_file_pick

Finds files under the current directory using fd (or find as fallback) and presents them in an fzf picker with a bat preview. Prints the selected path to stdout.

### Usage
```bash
zi_file_pick    # pick a file interactively and print its path
```
