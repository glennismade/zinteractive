---
category: Git
description: commit with message prompt
---
# zi_git_commit

Prompts for a commit message and runs `git commit -m`. Aborts cleanly if an empty message is entered.

### Usage
```bash
zi_git_commit    # prompt for message and commit
```
