---
category: Git
description: git push
---
# zi_git_push

Pushes the current branch to its configured remote. Requires being inside a git repository.

### Usage
```bash
zi_git_push    # push current branch to remote
```
