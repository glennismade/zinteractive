---
category: Git
description: stage all + commit as 'wip'
---
# zi_git_wip

Runs `git add -A && git commit -m "wip"` in one shot. Useful for quickly saving work in progress without writing a proper commit message.

### Usage
```bash
zi_git_wip    # stage everything and commit as 'wip'
```
