---
category: Git
description: interactive git log with preview
---
# zi_git_log

Shows the last 50 commits in an fzf picker with a full `git show` preview pane. Falls back to plain `git log --oneline` if fzf is unavailable.

### Usage
```bash
zi_git_log    # browse recent commits interactively
```
