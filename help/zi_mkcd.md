---
category: Nav
description: create directory and cd into it
---
# zi_mkcd

Creates a directory (including any missing parent directories) and immediately changes into it. Saves the two-step `mkdir -p` + `cd` dance.

### Usage
```bash
zi_mkcd my/new/dir    # create directory and cd into it
```
