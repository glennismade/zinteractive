---
category: Git
description: bat-powered git diff
---
# zi_git_diff

Runs `git diff` with color output. When bat is available the diff is piped through bat for syntax-highlighted display.

### Usage
```bash
zi_git_diff    # show working tree diff, highlighted with bat if available
```
