---
category: Git
description: git status
---
# zi_git_status

Runs `git status` in the current repository. Exits with a message if not inside a git repo.

### Usage
```bash
zi_git_status    # show working tree status
```
