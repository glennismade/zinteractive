---
category: Git
description: fzf branch switcher / create + push
---
# zi_git_checkout

Opens an fzf picker with all local branches and a log preview. Type a new branch name to create it, or select an existing one to switch. Offers to push newly created branches to origin.

### Usage
```bash
zi_git_checkout    # pick or create a branch interactively
```
