---
category: Git
description: undo last commit keep changes
---
# zi_git_oops

Shows the last commit and asks for confirmation before running `git reset --soft HEAD~1`. Changes are kept staged so nothing is lost.

### Usage
```bash
zi_git_oops    # undo last commit, keep changes staged
```
